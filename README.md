# Flask + Polars Mini App

A very small demo app using **Flask** (for the backend) and **Polars** (for data handling).

## 🚀 Getting started

### 1. Clone the repo

```bash
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>
```
